[project]
name = "agent-interview"
version = "0.1.0"
description = "同一个文献综述业务的三种 Agent 形态：写死的 workflow、裸 ReAct、agent harness"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"
dependencies = [
    "deepagents>=0.7.9",
    "httpx>=0.28.1",
    "langchain-openai>=1.6.0",
    "langgraph>=1.2.11",
    "python-dotenv>=1.2.3",
]
