[project]
name = "paper-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "deepagents>=0.7.9",
    "httpx>=0.28.1",
    "langchain-openai>=1.6.0",
    "langgraph>=1.2.11",
    "python-dotenv>=1.2.3",
]
